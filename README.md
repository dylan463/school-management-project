# React + Vite

### installation
- installer les dépendance du serveur backend
- installer les dépendance du serveur frontend

- modifier les parametre dans setting (pour une mise en production)
- creer un .env et y mettre toutes les information sensible (backend et frontend possède leur propre .env)
    info base de donnée
    info serveur smtp
    url pour le serveur de frontend
    autre info sensible

